# Patches the Electron installer to use a Zip extraction library
# that works with newer NodeJS versions.
# See https://github.com/max-mapper/extract-zip/issues/154
diff --git a/install.js b/install.js
index 540e1e68a561fbd0b3af0438b0465e984a7d5b52..4f4c624412bdaabf4706fd26c9278ee5e58381b3 100755
--- a/install.js
+++ b/install.js
@@ -2,8 +2,7 @@
 
 const { downloadArtifact } = require('@electron/get');
 
-const extract = require('extract-zip');
-
+const AdmZip = require('adm-zip');
 const childProcess = require('child_process');
 const fs = require('fs');
 const os = require('os');
@@ -79,20 +78,22 @@ function isInstalled() {
 function extractFile(zipPath) {
   const distPath = process.env.ELECTRON_OVERRIDE_DIST_PATH || path.join(__dirname, 'dist');
 
-  return extract(zipPath, { dir: path.join(__dirname, 'dist') }).then(() => {
-    // If the zip contains an "electron.d.ts" file,
-    // move that up
-    const srcTypeDefPath = path.join(distPath, 'electron.d.ts');
-    const targetTypeDefPath = path.join(__dirname, 'electron.d.ts');
-    const hasTypeDefinitions = fs.existsSync(srcTypeDefPath);
+  const dir = path.join(__dirname, 'dist');
+  const zip = new AdmZip(zipPath);
+  zip.extractAllTo(dir, false, true);
 
-    if (hasTypeDefinitions) {
-      fs.renameSync(srcTypeDefPath, targetTypeDefPath);
-    }
+  // If the zip contains an "electron.d.ts" file,
+  // move that up
+  const srcTypeDefPath = path.join(distPath, 'electron.d.ts');
+  const targetTypeDefPath = path.join(__dirname, 'electron.d.ts');
+  const hasTypeDefinitions = fs.existsSync(srcTypeDefPath);
 
-    // Write a "path.txt" file.
-    return fs.promises.writeFile(path.join(__dirname, 'path.txt'), platformPath);
-  });
+  if (hasTypeDefinitions) {
+    fs.renameSync(srcTypeDefPath, targetTypeDefPath);
+  }
+
+  // Write a "path.txt" file.
+  return fs.promises.writeFile(path.join(__dirname, 'path.txt'), platformPath);
 }
 
 function getPlatformPath() {
diff --git a/package.json b/package.json
index 3ce80683f884cf3b2281abbdcddc7b201077d3b5..4faba150642cfb2cb5ecab9e84274dbf8ac74848 100644
--- a/package.json
+++ b/package.json
@@ -30,6 +30,6 @@
   "dependencies": {
     "@electron/get": "^5.0.0",
     "@types/node": "^24.9.0",
-    "extract-zip": "^2.0.1"
+    "adm-zip": "0.5.16"
   }
 }
\ No newline at end of file

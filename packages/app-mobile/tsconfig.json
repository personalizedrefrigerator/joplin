{
	"extends": "../../tsconfig.json",
	"include": [
		"**/*.ts",
		"**/*.tsx",
	],
	"exclude": [
		//Files that don't need transpilation
		"**/node_modules",
		"gulpfile.ts",
		"tools/*.ts",
		"specs/*.ts"
	],
	"compilerOptions": {
		"types": ["jest", "node"],
		"moduleSuffixes": [".ios", ".android", ".native", ""]
	}
}

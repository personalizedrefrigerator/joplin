{
  "name": "<%= packageName %>",
  "version": "1.0.0",
  "scripts": {
    "dist": "node build.mts build",
    "prepare": "npm run dist",
    "updateVersion": "node build.mts bump-version",
    "update": "npm install -g generator-joplin && yo joplin --node-package-manager npm --update --force"
  },
  "license": "MIT",
  "keywords": [
    "joplin-plugin"
  ],
  "files": [
    "publish"
  ],
  "devDependencies": {
    "@types/node": "^18.7.13",
    "glob": "^8.0.3",
    "tar": "^6.1.11",
    "typescript": "^4.8.2",
    "esbuild": "0.25.11"
  }
}
